# Обновление: Вариант 3 (Гибридная структура) ✅

## Что изменилось

### 🎯 Оценка из 10, а не из 50

**Раньше:**
- За каждый правильный ответ: +5 баллов
- Максимум: 50 баллов (10 вопросов × 5)

**Теперь:**
- За каждый правильный ответ: +1 балл
- Максимум: 10 баллов (10 вопросов × 1)
- Проще и понятнее! 🎉

---

## 📊 Новая структура базы данных

### Таблица `quiz_sessions`

Теперь сохраняет:
- `correct_answers` - Количество правильных (0-10)
- `total_questions` - Всего вопросов (10)
- `percentage` - Процент правильных (0-100%)
- `all_answers` - **JSONB с полными данными всех ответов**

### Пример данных:

```json
{
  "first_name": "Денис",
  "last_name": "Безверхий",
  "correct_answers": 7,
  "total_questions": 10,
  "percentage": 70,
  "all_answers": [
    {
      "question_id": "01",
      "question_text": "Какое название...",
      "selected_answer": "React.js",
      "correct_answer": "Все вышеперечисленное",
      "is_correct": false
    },
    // ... остальные 9 вопросов
  ]
}
```

---

## 🚀 Что нужно сделать

### ШАГ 1: Обновить таблицы в Supabase

1. Откройте SQL Editor:
   ```
   https://supabase.com/dashboard/project/nkmkbflbkdadhjwlydvz/sql
   ```

2. Скопируйте содержимое файла:
   ```
   database/setup.sql
   ```

3. Вставьте в SQL Editor и нажмите **Run**

4. Проверьте результат:
   ```
   table_name     | row_count
   ---------------|-----------
   quiz_sessions  | 0
   quiz_answers   | 0
   ```

### ШАГ 2: Перезапустите приложение

```bash
# Остановите текущий процесс (Ctrl+C)
# Запустите заново
npm start
```

### ШАГ 3: Протестируйте

1. Введите имя и фамилию
2. Пройдите квиз
3. Проверьте результат в Supabase Table Editor

---

## ✨ Преимущества новой структуры

### 1. Оценка понятнее
- **7 из 10** вместо **35 из 50**
- Сразу видно процент: **70%**

### 2. Все данные в одном месте
- Одним запросом получаете полную информацию
- Поле `all_answers` содержит ВСЕ ответы ученика

### 3. Легкая аналитика
- Таблица `quiz_answers` для статистики по вопросам
- Можно найти самые сложные вопросы
- Можно увидеть популярные ошибки

### 4. Быстрый доступ
- Не нужно делать JOIN для получения ответов
- Все в JSONB поле `all_answers`

---

## 📖 Полезные файлы

### `database/setup.sql`
Основной скрипт для создания таблиц

### `database/useful-queries.sql`
14+ готовых SQL запросов для учителя:
- Просмотр всех результатов
- Рейтинг учеников
- Статистика по вопросам
- Экспорт для Excel
- И многое другое

### `database/README.md`
Подробная документация по структуре БД

---

## 🔍 Примеры запросов

### Все результаты

```sql
SELECT 
  first_name || ' ' || last_name as ученик,
  correct_answers || ' из ' || total_questions as результат,
  percentage || '%' as процент
FROM quiz_sessions
WHERE completed_at IS NOT NULL
ORDER BY percentage DESC;
```

### Детальные ответы ученика

```sql
SELECT 
  jsonb_pretty(all_answers) as ответы
FROM quiz_sessions
WHERE first_name = 'Денис' AND last_name = 'Безверхий';
```

### Рейтинг ТОП-10

```sql
SELECT 
  ROW_NUMBER() OVER (ORDER BY correct_answers DESC) as место,
  first_name || ' ' || last_name as ученик,
  correct_answers || ' из 10' as результат,
  percentage || '%' as процент
FROM quiz_sessions
WHERE completed_at IS NOT NULL
ORDER BY correct_answers DESC
LIMIT 10;
```

---

## ⚠️ Важно!

### Старые данные будут удалены!

При выполнении `setup.sql` старые таблицы удаляются командой:
```sql
DROP TABLE IF EXISTS quiz_answers CASCADE;
DROP TABLE IF EXISTS quiz_sessions CASCADE;
```

Если хотите сохранить старые данные:
1. Экспортируйте их через Table Editor
2. Или создайте backup:
   ```sql
   CREATE TABLE quiz_sessions_backup AS SELECT * FROM quiz_sessions;
   ```

---

## 🎉 Готово!

После обновления у вас будет:
- ✅ Понятная оценка из 10
- ✅ Процент правильных ответов
- ✅ Все ответы в одном месте (JSONB)
- ✅ Детальная аналитика (quiz_answers)
- ✅ Готовые SQL запросы для учителя

Удачи! 🚀
